<h2><%= t :tasks_list %></h2>
<h5><%= notice %></h5>
<%= link_to "#{t :new_task}", new_task_path, class: 'btn btn-outline-primary' %>
<% @tasks.each do |task| %>
  <div class="card">
    <div class="card-header">
      <a><%= task.title %></a>
      <a><%= task.created_at %></a>
    </div>
    <div class="card-body">
      <p class="card-text">
        <%= task.content %>
      </p>
      <%= link_to "#{t :edit_task}", edit_task_path(task), class: 'btn btn-outline-primary' %>
      <%= link_to "#{t :delete_task}", task_path(task), method: 'delete', data: { confirm: "#{I18n.t :delete_sure}"}, class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
